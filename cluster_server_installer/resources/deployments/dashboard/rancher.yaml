apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: rancher
  namespace: cattle-system
spec:
  repo: https://releases.rancher.com/server-charts/latest
  chart: rancher
  targetNamespace: cattle-system
  version: 2.8.0
  valuesContent: |-
    hostname: dashboard.ronenv96.cloud-iy.com
    bootstrapPassword: admin
    ingress:
     tls:
       source: letsEncrypt
    letsEncrypt:
     email: ronen4822@gmail.com
     ingress:
       class: traefik